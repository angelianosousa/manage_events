<!DOCTYPE html>
<html>
  <head>
    <title>ManageEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body id="body_bg">
    <%= render '/company_module/shared/navbar' %>

    <div class="container-fluid mb-5">
      <%= render '/shared/notice_messages' %>

      <%= content_for(:page_header) || 'Sistema de Gerenciamento' %>

      <%= yield %>
    </div>

    <%= javascript_importmap_tags %>
  </body>
</html>
